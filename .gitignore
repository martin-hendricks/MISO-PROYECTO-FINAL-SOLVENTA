# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node / Angular
node_modules/
dist/
.angular/

# Android
*.iml
.gradle/
/local.properties
/.idea/
.cxx/
build/
captures/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Experimentos
# (results/raw/ SÍ se versiona — son JSON/CSV/TXT pequeños (~4MB) que el
# INFORME.md de HA-08 cita como fuente auditable de cada cifra reportada;
# excluirlos hacía que el informe no fuera verificable para quien clona el
# repo sin haber corrido las corridas. El dataset generado y los exports
# pesados de Prometheus siguen fuera del repo por su propio tamaño.)

# OS / editores
.DS_Store
*.swp
.vscode/
